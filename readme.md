# Bitrix, редакция "Старт"

После клонирования, требуется дополнительно выполнить:

```bash
cd ./www/
git clone --branch bitrix --single-branch git@github.com:xnovikov/bitrix-docker.git www/bitrix 
```
